apiVersion: v1
kind: Service

metadata:
  name: my-app
  namespace: my-app
  labels:
    app: my-app

spec:
  type: ClusterIP

  selector:
    app: my-app

  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: http
